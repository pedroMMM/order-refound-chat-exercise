[project]
name = "refund-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "langgraph>=0.2",
    "langchain>=0.3",
    "langchain-core>=0.3",
    "langfuse>=2.0",
    "pydantic>=2.0",
]

